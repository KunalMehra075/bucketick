# @bucketick/mobile (Phase 3)

Mobile app — Expo + React Native + TS + NativeWind + React Navigation + Zustand +
TanStack Query + React Hook Form.

Not built yet. Consumes `@bucketick/design-tokens` (TS token object via NativeWind) and
`@bucketick/api-client`.

Screens: Home Feed, Explore, Create Bucket List, Profile, Notifications, Messages, Search,
Achievements, Saved, Settings.
